@page "/"
@inject SimpleGpioService _gpio
@rendermode InteractiveServer

<h1>GPIO Pins @(IGpioControllerUser.HasGpioController ? string.Empty : "(Dummy)")</h1>

@foreach (var pin in _gpio.Pins)
{
    <SimpleGpioPin Pin=pin />
}
